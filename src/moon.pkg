import {
  "justjavac/ffi",
}

supported_targets = "+native"

options(
  "is-main": true,
  link: { "native": { "cc-flags": "", "cc-link-flags": "user32.lib /DEBUG" } },
  targets: { "main.mbt": [ "native" ] },
)
